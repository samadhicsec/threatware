---
output-texts:
  default:
    success: "Successfully converted document '{{ request.docloc }}' using scheme '{{ request.scheme }}'"
    unknown-doc-storage: "Unknown document storage type '{{ doc_storage }}' in scheme '{{ request.scheme }}'"