---
# The mapping of validator name to python file is kept separate to the configuration of each validator as the security requirements are different
# i.e. who can specify what python code is different is likely different to who can create a new validator config that leverages an existing validator.
validators:
  test_validator: "test.validators.test_validator"
